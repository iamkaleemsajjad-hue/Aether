# Benchmark engine dependencies
#
# 1. Aether from source: pip install -e ".[pytorch]"
#
# 2. Core deps
transformers>=4.40.0
accelerate>=0.30.0
psutil>=5.9.0
nvidia-ml-py>=12.535.0
matplotlib>=3.7.0
huggingface-hub>=0.27.0
packaging>=24.0
#
# 3. ONNX Runtime + CUDA
#
#   !!! GPU BUILD REQUIRED FOR GPU BENCHMARKS !!!
#
#   The CPU build (onnxruntime) and the GPU build (onnxruntime-gpu) CANNOT
#   coexist. On Kaggle and many cloud images the CPU build is pre-installed.
#   You MUST remove it first or pip will skip the GPU wheel entirely (the
#   onnxruntime dependency is already satisfied), leaving CUDAExecutionProvider
#   absent and the engine silently running on CPU.
#
#   Correct install sequence (no session restart required — probe and workers
#   run in subprocesses that always see the current disk state):
#     pip uninstall -y onnxruntime                       # remove CPU build FIRST
#     pip install "onnxruntime-gpu>=1.18.0"              # install GPU build
#     pip install "optimum[onnxruntime]>=1.20.0"         # [onnxruntime] extra REQUIRED
#
#   The [onnxruntime] extra is required: plain "optimum" does not include the
#   optimum.onnxruntime subpackage. pip sees onnxruntime as satisfied by the
#   GPU build (onnxruntime-gpu >=1.18 registers as "onnxruntime") and does NOT
#   reinstall the CPU build.
#
#   Verify the GPU build is active:
#     python -c "import onnxruntime as ort; print(ort.get_available_providers())"
#     # Must print CUDAExecutionProvider in the list
#
#   On a GPU host, set AETHER_ORT_REQUIRE_GPU=1 to treat a missing GPU build as
#   NOT_INSTALLED (excludes the engine rather than running it on CPU):
#     export AETHER_ORT_REQUIRE_GPU=1
#     python benchmark.py ...
#
#   Pre-check optimum/transformers version conflicts:
#     python -c "from benchmark.suite.engines.base import requirement_conflicts; \
#                print(requirement_conflicts('optimum'))"
#
optimum[onnxruntime]>=1.20.0
#
# 4. llama.cpp + CUDA
#   Pre-built (CUDA 12.x, no compiler needed):
#     pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124
#   Build from source:
#     PowerShell: $env:CMAKE_ARGS="-DGGML_CUDA=on"
#     pip install llama-cpp-python --no-binary llama-cpp-python
#   Verify: python -c "from llama_cpp import Llama; print('OK')"
#   GGUF: pass --gguf-convert-script / --gguf-dir / --gguf-map to benchmark.py
#
# 5. Aether Runtime: pip install -e ".[pytorch]"  (same as step 1)
