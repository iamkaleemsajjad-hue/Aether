# Benchmark engine dependencies
#
# 1. Aether from source: pip install -e ".[pytorch]"
#
# 2. Core deps
transformers>=4.40.0
accelerate>=0.30.0
psutil>=5.9.0
nvidia-ml-py>=12.535.0
matplotlib>=3.7.0
huggingface-hub>=0.27.0
packaging>=24.0
#
# 3. ONNX Runtime + CUDA
#   pip install onnxruntime-gpu>=1.18.0
#   pip install "optimum[onnxruntime-gpu]>=1.20.0"
#   Never install onnxruntime (CPU) and onnxruntime-gpu together -- they conflict.
#   Pre-check version conflicts:
#     python -c "from benchmark.suite.engines.base import requirement_conflicts;
#                print(requirement_conflicts('optimum'))"
optimum>=1.20.0
#
# 4. llama.cpp + CUDA
#   Pre-built (CUDA 12.x, no compiler needed):
#     pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu124
#   Build from source:
#     PowerShell: $env:CMAKE_ARGS="-DGGML_CUDA=on"
#     pip install llama-cpp-python --no-binary llama-cpp-python
#   Verify: python -c "from llama_cpp import Llama; print('OK')"
#   GGUF: pass --gguf-convert-script / --gguf-dir / --gguf-map to benchmark.py
#
# 5. Aether Runtime: pip install -e ".[pytorch]"  (same as step 1)
